--- a/hw/display/sm501.c	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/display/sm501.c	2026-09-20 08:41:19.640376516 -0600
@@ -2164,6 +2164,13 @@
     .instance_init = sm501_sysbus_init,
 };
 
+/*
+ * The PCI variant is only buildable where the PCI core is present.
+ * Boards such as m68k/sage040 use the sysbus variant on a machine with
+ * no PCI bus at all, and must not drag the PCI subsystem into the link.
+ */
+#ifdef CONFIG_PCI
+
 #define TYPE_PCI_SM501 "sm501"
 OBJECT_DECLARE_SIMPLE_TYPE(SM501PCIState, PCI_SM501)
 
@@ -2251,10 +2258,14 @@
     },
 };
 
+#endif /* CONFIG_PCI */
+
 static void sm501_register_types(void)
 {
     type_register_static(&sm501_sysbus_info);
+#ifdef CONFIG_PCI
     type_register_static(&sm501_pci_info);
+#endif
 }
 
 type_init(sm501_register_types)
--- a/target/m68k/cpu.h	2026-08-27 06:23:32.000000000 -0600
+++ b/target/m68k/cpu.h	2026-09-20 08:55:37.665983150 -0600
@@ -154,6 +154,18 @@
 
     /* Fields from here on are preserved across CPU reset. */
     uint64_t features;
+
+    /*
+     * Optional interrupt-acknowledge callback.  A vectored interrupt
+     * controller (e.g. the MC68901 MFP) registers this so it can perform
+     * the side effects real silicon performs during the IACK cycle -
+     * clearing the pending bit and flagging the channel in service.
+     *
+     * This must live below end_reset_fields: it is installed once when
+     * the controller is realized, and a CPU reset must not erase it.
+     */
+    void (*iack_handler)(void *opaque, int level);
+    void *iack_opaque;
 } CPUM68KState;
 
 /*
@@ -472,6 +484,9 @@
 #define MACSR_EV    0x001
 
 void m68k_set_irq_level(M68kCPU *cpu, int level, uint8_t vector);
+void m68k_set_iack_handler(M68kCPU *cpu,
+                           void (*handler)(void *opaque, int level),
+                           void *opaque);
 void m68k_switch_sp(CPUM68KState *env);
 
 void do_m68k_semihosting(CPUM68KState *env, int nr);
--- a/target/m68k/helper.c	2026-08-27 06:23:32.000000000 -0600
+++ b/target/m68k/helper.c	2026-09-20 08:45:33.676509022 -0600
@@ -940,6 +940,14 @@
  * the vector when the interrupt is acknowledged by the CPU.  For
  * simplicity we calculate it when the interrupt is signalled.
  */
+void m68k_set_iack_handler(M68kCPU *cpu,
+                           void (*handler)(void *opaque, int level),
+                           void *opaque)
+{
+    cpu->env.iack_handler = handler;
+    cpu->env.iack_opaque = opaque;
+}
+
 void m68k_set_irq_level(M68kCPU *cpu, int level, uint8_t vector)
 {
     CPUState *cs = CPU(cpu);
--- a/target/m68k/op_helper.c	2026-08-27 06:23:32.000000000 -0600
+++ b/target/m68k/op_helper.c	2026-09-20 08:55:37.667381556 -0600
@@ -531,7 +531,16 @@
          * first signalled.
          */
         cs->exception_index = env->pending_vector;
+        /*
+         * Exception entry first: do_interrupt_all() reads pending_level
+         * to set the new interrupt mask in SR, so the acknowledge
+         * callback must not disturb it until that has happened.
+         */
         do_interrupt_m68k_hardirq(env);
+        if (env->iack_handler) {
+            env->iack_handler(env->iack_opaque,
+                              (env->sr & SR_I) >> SR_I_SHIFT);
+        }
         return true;
     }
     return false;
--- a/hw/m68k/Kconfig	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/m68k/Kconfig	2026-09-21 10:50:27.796342309 -0600
@@ -46,3 +46,15 @@
     select GOLDFISH_TTY
     select GOLDFISH_RTC
     select VIRTIO_MMIO
+
+config SAGE040
+    bool
+    default y
+    depends on M68K
+    select MC68901          # MFP: timers + vectored interrupt controller
+    select SERIAL_MM        # NS16550A console
+    select IDE_MMIO         # ATA taskfile disk
+    select SMC91C111        # SMSC LAN91C111 ethernet
+    select SM501            # bitmapped video
+    select M48T59           # RTC + 8 KB NVRAM
+    select PCKBD            # 8042 keyboard controller, memory mapped
--- a/hw/m68k/meson.build	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/m68k/meson.build	2026-09-19 20:27:11.884302502 -0600
@@ -5,4 +5,6 @@
 m68k_ss.add(when: 'CONFIG_Q800', if_true: files('q800.c', 'q800-glue.c'))
 m68k_ss.add(when: 'CONFIG_M68K_VIRT', if_true: files('virt.c'))
 
+m68k_ss.add(when: 'CONFIG_SAGE040', if_true: files('sage040.c'))
+
 hw_arch += {'m68k': m68k_ss}
--- a/hw/misc/Kconfig	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/misc/Kconfig	2026-09-20 08:30:07.967887845 -0600
@@ -258,3 +258,7 @@
     bool
 
 source macio/Kconfig
+
+config MC68901
+    bool
+    depends on M68K
--- a/hw/misc/meson.build	2026-08-27 06:23:32.000000000 -0600
+++ b/hw/misc/meson.build	2026-09-20 08:30:07.949887732 -0600
@@ -1,3 +1,4 @@
+system_ss.add(when: 'CONFIG_MC68901', if_true: files('mc68901.c'))
 system_ss.add(when: 'CONFIG_APPLESMC', if_true: files('applesmc.c'))
 system_ss.add(when: 'CONFIG_EDU', if_true: files('edu.c'))
 system_ss.add(when: 'CONFIG_FW_CFG_DMA', if_true: files('vmcoreinfo.c'))
--- a/hw/input/Kconfig	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/input/Kconfig	2026-09-21 10:50:27.791149209 -0600
@@ -4,7 +4,6 @@
 config PCKBD
     bool
     select PS2
-    depends on ISA_BUS
 
 config PL050
     bool
--- a/hw/input/pckbd.c	2026-08-27 06:23:31.000000000 -0600
+++ b/hw/input/pckbd.c	2026-09-21 10:50:27.791960111 -0600
@@ -769,6 +769,15 @@
     .class_init    = i8042_mmio_class_init
 };
 
+/*
+ * The ISA variant.  Guarded because this file also provides i8042-mmio,
+ * which is a sysbus device and needs no ISA bus -- a board that has one
+ * but not the other would otherwise fail to link on isa_register_ioport
+ * and on the ACPI AML builder.  Same shape as the CONFIG_PCI guard in
+ * hw/display/sm501.c, and for the same reason.
+ */
+#ifdef CONFIG_ISA_BUS
+
 void i8042_isa_mouse_fake_event(ISAKBDState *isa)
 {
     KBDState *s = &isa->kbd;
@@ -964,9 +973,13 @@
     },
 };
 
+#endif /* CONFIG_ISA_BUS */
+
 static void i8042_register_types(void)
 {
+#ifdef CONFIG_ISA_BUS
     type_register_static(&i8042_info);
+#endif
     type_register_static(&i8042_mmio_info);
 }
 
