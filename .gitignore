# Build artifacts
*.elf
*.bin
*.o

# Emulator output captured during test runs
*.out
*.usart
*.usartin
*.ppm

# Disk image created on demand by the test Makefile
disk.img

# Serial capture from boot/run.sh
boot/out.txt

# Boot disk image and the flattened kernel written into it
bootrom/hd.img
bootrom/kernel.rom
